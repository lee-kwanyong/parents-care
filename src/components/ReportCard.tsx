import type { CareReport } from '@/lib/types'
import { Card, CardTitle } from './Card'
function ReportSection({ title, items, highlight }: { title: string; items: string[]; highlight?: boolean }) { return <div className={highlight ? 'rounded-2xl bg-care-50 p-4' : ''}><h3 className="font-black">{title}</h3><ul className="mt-2 space-y-2 text-sm leading-6 text-[#4E6D69]">{items.map((item) => <li key={item}>• {item}</li>)}</ul></div> }
export function ReportCard({ report }: { report: CareReport }) { return <Card><CardTitle eyebrow="30초 요약" title="보호자 리포트" description={report.summary} /><div className="grid gap-4 md:grid-cols-2"><ReportSection title="진료 진행 내용" items={report.progress} /><ReportSection title="의료진 안내사항" items={report.medicalGuidance} /><ReportSection title="검사·약·다음 예약" items={[...report.testsAndMedication, ...report.nextAppointment]} /><ReportSection title="비용" items={report.costs} /><ReportSection title="부모님 컨디션" items={[report.condition]} /><ReportSection title="가족이 해야 할 다음 액션" items={report.nextActions} highlight /></div>{report.managerNote ? <p className="mt-4 rounded-2xl bg-slate-50 p-4 text-sm leading-6 text-[#63807C]">매니저 메모: {report.managerNote}</p> : null}</Card> }
