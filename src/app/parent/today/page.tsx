import { ParentTodayPersistent } from '@/components/parent/ParentTodayPersistent'

export const metadata = {
  title: '오늘 안부 | 부모님 안심케어',
  description: '부모님이 오늘 안부를 보호자에게 전달합니다.'
}

export default function ParentTodayPage() {
  return <ParentTodayPersistent />
}
