import { AppShell } from '@/components/AppShell'
import { Card, CardTitle } from '@/components/Card'
export default function Page() { return <AppShell title="매니저 지원서" subtitle="차량 보유 여부와 직접 운송 가능 여부를 분리해 입력합니다."><Card><CardTitle title="지원서 접수" description="가능 지역, 전문분야, 경력, 자격, 차량 보유 여부, 직접 운송 계약 검증 여부를 분리합니다." /></Card></AppShell> }
