import { AppShell } from '@/components/AppShell'
import { ManagerTodayConsole } from '@/components/ManagerTodayConsole'
export default function ManagerTodayPage() { return <AppShell title="동행매니저앱" subtitle="현장에서는 체크·사진·짧은 메모만. 복잡한 입력은 운영실이 정리합니다."><ManagerTodayConsole /></AppShell> }
