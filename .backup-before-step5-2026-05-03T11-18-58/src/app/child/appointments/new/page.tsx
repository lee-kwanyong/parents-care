import { AppShell } from '@/components/AppShell'
import { Card, CardTitle } from '@/components/Card'
import { pickupModeLabels } from '@/lib/constants'
export default function NewAppointmentPage() { const modes = Object.entries(pickupModeLabels) as [string, string][]; return <AppShell title="병원 일정 등록" subtitle="직접 긴 폼을 쓰기 어려우면 사진·카톡·전화로 맡길 수 있습니다."><Card><CardTitle eyebrow="간단 입력" title="필수 정보만 먼저 받습니다" /><form className="grid gap-4"><input className="rounded-2xl border border-slate-200 p-4" placeholder="병원명" /><input className="rounded-2xl border border-slate-200 p-4" placeholder="예약일시" /><select className="rounded-2xl border border-slate-200 p-4">{modes.map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select><textarea className="min-h-32 rounded-2xl border border-slate-200 p-4" placeholder="걱정되는 점이나 병원 예약 문자 내용을 붙여넣으세요" /><button className="rounded-3xl bg-care-500 p-5 text-xl font-black text-white" type="button">일정 맡기기</button></form></Card></AppShell> }
