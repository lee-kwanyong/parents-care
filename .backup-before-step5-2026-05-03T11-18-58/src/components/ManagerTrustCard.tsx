import type { ManagerTrustCard as ManagerTrustCardType } from '@/lib/types'
import { VehiclePolicyBadge } from './VehiclePolicyBadge'
export function ManagerTrustCard({ manager }: { manager: ManagerTrustCardType }) { return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-soft"><div className="flex items-start justify-between gap-4"><div><p className="text-sm font-bold text-care-700">만나는 매니저</p><h3 className="mt-1 text-2xl font-black">{manager.name}</h3><p className="mt-2 text-sm text-slate-600">완료 {manager.completedCount}건 · 안심도 {manager.trustScore}점</p></div><span className="rounded-full bg-care-100 px-4 py-2 text-sm font-black text-care-700">신뢰카드</span></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><p className="rounded-2xl bg-slate-50 p-3 text-sm">차량 보유: <strong>{manager.hasVehicle ? '있음' : '없음'}</strong></p><p className="rounded-2xl bg-slate-50 p-3 text-sm">직접 운송: <strong>{manager.directTransportAvailable ? '별도 검토 필요' : '기본 미포함'}</strong></p></div><div className="mt-4 flex flex-wrap gap-2">{[...manager.specialties, ...manager.verifiedBadges].map((item) => <span key={item} className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold text-slate-600">{item}</span>)}</div><div className="mt-4"><VehiclePolicyBadge /></div></div> }
