import { AppShell } from '@/components/AppShell'
import { Card, CardTitle } from '@/components/Card'
export default function ParentInstallPage() { return <AppShell title="부모님 폰 설치 안내" subtitle="PWA로 홈 화면에 추가해 앱처럼 사용할 수 있게 합니다."><Card><CardTitle title="설치 순서" description="브라우저 메뉴 → 홈 화면에 추가 → 부모님케어 아이콘 실행. 부모님은 큰 글씨 화면만 보게 됩니다." /></Card></AppShell> }
